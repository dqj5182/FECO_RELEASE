## Data
You need to follow directory structure of the `data` as below.
```
${ROOT} 
|-- data  
|   |-- base_data
```
* Download `base_data` from HuggingFace by running:
```
bash scripts/download_demo_base_data.sh
```